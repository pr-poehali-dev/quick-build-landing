-- Упрощаем схему: теперь form_id зависит только от типа формы (одинаковый для всех страниц).
-- Помечаем канонические 3 строки (уже существующие с id 300/301/302) как общие для всех страниц.
UPDATE uis_form_registry SET category = 'Все страницы' WHERE form_id IN (300, 301, 302);
